import React from 'react';
import './Header.css';

export default function Header() {
  return (
    <header className="header">
      <div className="header-brand">
        <span className="header-title">Syntax Error Detector</span>
        <span className="header-subtitle">Grammar & Syntax Analyzer</span>
      </div>

    </header>
  );
}
